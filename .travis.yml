language: node_js
node_js:
 - "node"
sudo: required
env:
  - NODE_ENV='test'
before_script:
  - npm install -g react-native-cli
  - npm install --save-dev jest-react-native
  - npm install --save-dev babel-cli
script:
  - npm test
notifications:
  email: false
